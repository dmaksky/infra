apiVersion: v2
name: prometheus
description: "Helm-чарт для развёртывания Prometheus (сбор метрик кластера и логов)"
type: application
version: "0.1.0"
appVersion: "v2.44.0"
dependencies:
  - name: prometheus
    repository: "https://prometheus-community.github.io/helm-charts"
    version: "27.16.0"
